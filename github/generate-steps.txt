
--------------------------------------------------------------------------
Gerar a Chave SSH
--------------------------------------------------------------------------
Windows
ssh-keygen -t rsa -b 4096 -C "anthony@hcode.com.br"

Linux
ssh-keygen -t rsa -b 4096 -C "dev-fix@fix.com.br"

Mac
ssh-keygen -t rsa -b 4096 -C "dev-fix@fix.com.br"
--------------------------------------------------------------------------

--------------------------------------------------------------------------
Copiar a Chave SSH
--------------------------------------------------------------------------
Windows
clip < ~/.ssh/id_rsa.pub

Mac
pbcopy < ~/.ssh/id_rsa.pub

Linux
sudo apt-get install xclip
xclip -sel clip < ~/.ssh/id_rsa.pub
--------------------------------------------------------------------------